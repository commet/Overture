// Same image as opengraph-image — Next.js will auto-route Twitter scrapes here.
// Kept as a re-export so we never drift between OG and Twitter previews.
export { default, alt, size, contentType } from './opengraph-image';
